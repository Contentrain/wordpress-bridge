# Security policy

Report vulnerabilities privately to `security@contentrain.io`. Do not include production credentials, personal data, or a complete WordPress export in a report.

The export endpoint requires WordPress' `export` capability and a request nonce. This release has no inbound public endpoint and makes no outbound network request.
